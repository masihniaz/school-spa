// import { AbstractControl, ValidationErrors } from '@angular/forms';
// import { HttpClient } from '@angular/common/http';
// import { DataService } from '../services/data.service';
// import { Observable } from 'rxjs';

// // export class EmailValidators {
// //   url: 'http://localhost:4000/api/';
// //   constructor(private http: HttpClient) {}

// //   shouldBeUnique(control: AbstractControl, type: string) : Observable<ValidationErrors | null> {
// //     const headers = DataService.getAuthorizationHeader();
// //     const payload = { email: control.value};
// //     this.http.post(`${this.url}${type}/is-exist`, JSON.stringify(payload), { headers })
// //       .subscribe(response => {
// //         if (true) {
// //           return { shouldBeUnique: true};
// //         }
// //         return null;
// //       });
// //   }
// // }
